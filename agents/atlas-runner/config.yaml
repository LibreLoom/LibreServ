log:
  level: info
runner:
  file: /data/.runner
  capacity: 1
  timeout: 3h
  insecure: false
  fetch_interval: 2s
  labels:
    - atlas:host
cache:
  enabled: false
container:
  docker_host: "-"
  privileged: false
  network: bridge
host:
  workdir_parent: /data/jobs
