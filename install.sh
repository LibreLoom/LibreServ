#!/bin/bash
# ABANDONED branch tip — do not use this install.sh.
# Nightly 2026-09-15: curl --proto '=https' --tlsv1.2 harden for Forgejo release
# downloads is ready locally (commit 676ff099b) but could not be landed via GitHub
# MCP (~32KB file). Do not open/merge a PR from this branch. Use main.
echo "error: abandoned audit branch tip; use main install.sh" >&2
exit 1
