# OS Client Stack

Place the built frontend (SPA) here. The API now serves static assets from `./OS/dist`, so the CI/build should output into this directory (e.g., `OS/dist/index.html`, JS, CSS, and assets).

If you used to build into `web/dist`, update build scripts to target `OS/dist` instead.
