package net.plainskill.luna

import com.journeyapps.barcodescanner.CaptureActivity

/** QR scanner in portrait — the library default is landscape. */
class PortraitCaptureActivity : CaptureActivity()
