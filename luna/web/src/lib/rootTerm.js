/**
 * Gloss for TermHint on "root" when it means the drive's first folder level.
 * Keep this in sync wherever UI says "root" for filesystem meaning.
 */
export const ROOT_TERM_HINT =
  "The first folder level on the drive — the main folder you see when you open it.";
